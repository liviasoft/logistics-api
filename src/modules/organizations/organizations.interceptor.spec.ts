import { OrganizationsInterceptor } from './organizations.interceptor';

describe('OrganizationsInterceptor', () => {
  it('should be defined', () => {
    expect(new OrganizationsInterceptor()).toBeDefined();
  });
});
