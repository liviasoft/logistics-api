import { DevelopersInterceptor } from './developers.interceptor';

describe('DevelopersInterceptor', () => {
  it('should be defined', () => {
    expect(new DevelopersInterceptor()).toBeDefined();
  });
});
